<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mvplzl.contacts"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".functions.Add" android:label="增加功能"></activity>
        <activity android:name=".functions.Alter" android:label="修改功能"></activity>
        <activity android:name=".functions.Check" android:label="查看功能"></activity>
        <activity android:name=".functions.Delete" android:label="删除功能"></activity>
        <activity android:name=".functions.Dial" android:label="拨号功能"></activity>
        <activity android:name=".functions.child.SetNumber" android:label="修改"></activity>
        <activity android:name=".functions.child.CheckNumber" android:label="查看"></activity>
    </application>

</manifest>
